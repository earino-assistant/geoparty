Read /opt/data/geoparty/geoparty-spec.md completely. You are building the full GeoParty game to that spec, fully autonomously.

WORK SCOPE
- Work ONLY inside /opt/data/geoparty/ (it already contains geoparty-spec.md and tools/build_location_pool.py). Do not touch anything outside that directory.
- Follow section 3 "Repository layout" exactly and produce every file listed: index.html, host.html, screen.html, config.js, js/firebase.js, js/game.js, js/host-ui.js, js/screen-ui.js, js/pool.js, css/style.css, data/location_pool.json, tools/build_location_pool.py (keep unmodified), README.md.
- Implement everything in sections 1-12: architecture, data model, the two views (/host and /screen), game state machine, scoring (haversine + exponential decay), location pool loading/sampling, persistent leaderboard, visual design direction, edge cases.
- Use the real credentials from section 2 verbatim in config.js.
- Put the Firebase security rules text from section 5 into README.md. DO NOT attempt to access the Firebase console — that is a manual step for the owner, just document it.
- All asset paths must be relative (section 13.4).

LOCATION POOL
- Generate data/location_pool.json by running tools/build_location_pool.py with `--count 200 --mode weighted`. Install the `requests` dependency first (use pip or uv as available). If the Mapillary API fails, errors, or rate-limits, still ship the rest of the repo and clearly note in your final report that the pool could not be generated.

VERIFY
- Run whatever local verification you can from section 14 (e.g. syntax-check the JS, validate config.js and the pool JSON, sanity-check the HTML structure). Note any acceptance criteria that cannot be verified locally (they need the Firebase console or a live host — that's expected).

GIT
- After building, run git init, git add, and git commit with a clear descriptive message. Do NOT push to GitHub. Do NOT attempt Firebase console changes.

FINAL REPORT
Reply with: (1) list of files created, (2) what you verified, (3) any gaps or steps requiring manual action.
