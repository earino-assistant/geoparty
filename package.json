{
  "name": "geoparty",
  "version": "0.5.1",
  "private": true,
  "description": "Jackbox-style geoguessing party game — static, no-build, Firebase RTDB sync.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "check": "node --check config.js && for f in js/*.js tests/*.js tools/*.mjs; do node --check \"$f\" || exit 1; done"
  }
}
