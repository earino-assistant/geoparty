{
  "name": "geoparty",
  "version": "1.0.0",
  "private": true,
  "description": "Jackbox-style geoguessing party game — static, no-build, Firebase RTDB sync.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "check": "node --check config.js && for f in js/*.js tests/*.js; do node --check \"$f\" || exit 1; done"
  }
}
