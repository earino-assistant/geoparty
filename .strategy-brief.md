PROBLEM-SOLVING TASK — devise a BETTER way to build the GeoParty location pool, then execute it and finish the build.

CONTEXT
The GeoParty game code in /opt/data/geoparty/ is complete and verified (all files present: index.html, host.html, screen.html, config.js, css/style.css, js/*.js, README.md). Only the location pool and the git commit remain.

THE PROBLEM (this is what I want you to think hard about)
The provided `tools/build_location_pool.py` samples ONE random bbox per candidate and does a separate Mapillary Graph API query for each — so reaching a meaningful count means hundreds of serial API calls. The Mapillary API is flaky right now (frequent 500s / rate limits), so the naive `--count 100` run was still going after 30+ minutes and had yielded only a handful of panos. That approach is too slow to be practical today.

YOUR JOB
1. Come up with a BETTER strategy to obtain a solid, verified pool of Mapillary 360° pano locations — then implement and run it. I'm deliberately not prescribing the method; you're the engineer here. Think about what would be dramatically fewer, higher-yield API calls. Some directions you might consider (pick your own or invent a better one):
   - Query a small number of well-covered major cities / dense regions ONCE each with a high `limit` and `is_pano=true`, harvesting many verified panos per single request instead of one pano per request.
   - Batch multiple images per bbox request and pick many from each response.
   - Reuse the existing partial pool (5 entries already in /opt/data/geoparty/location_pool.json) as a seed and top it up efficiently.
   - If a clean API route is impractical, consider an alternative legitimate way to assemble a pool of valid Mapillary image IDs.
2. Constraints:
   - The repo must keep `tools/build_location_pool.py` unmodified (the spec says ship it as-is). You may ADD a new generator script if a faster method warrants it, or drive the API another way — just don't edit the provided tool.
   - Aim for a solid, PLAYABLE pool. ~10-30 is comfortably playable; more is nice but NOT worth hours. Do not grind for an exact count.
   - Output must go to `data/location_pool.json` in the spec's format: array of {image_id, lng, lat, viewer_url, thumb}.
   - Stay inside /opt/data/geoparty.
3. Finish: validate data/location_pool.json (non-empty, correct schema), then `git init` (if needed), `git add -A`, `git commit` with a clear message.
4. FINAL REPORT: (a) the STRATEGY you chose and why, (b) pool count shipped, (c) git commit hash, (d) any remaining manual steps. Do NOT push to GitHub. Do NOT touch the Firebase console.
