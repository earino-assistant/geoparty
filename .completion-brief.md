FOLLOW-UP TASK — finish the GeoParty build that you started. The code is complete and verified (index.html, host.html, screen.html, config.js, css/style.css, js/*.js, README.md are all present in /opt/data/geoparty/). Two things remain:

1. GENERATE THE LOCATION POOL
The previous attempt spun up 8 parallel background workers that hammered the Mapillary API and got 500 errors. Do it DIFFERENTLY this time:
- Run the generation SEQUENTIALLY, IN THE FOREGROUND (blocking), as a normal Bash command. Do NOT background it and do NOT end your turn while it runs — that is what caused the print-mode session to exit early last time.
- Command, run from /opt/data/geoparty:
  `uv run --with requests python3 tools/build_location_pool.py --count 100 --mode weighted`
  (use `pip install requests` + `python3` if uv is unavailable)
- This writes `location_pool.json` to the current directory when finished. The script rate-limits politely.
- A count of 100 is plenty (a game uses only 5-10 per session; the pool can be regenerated larger later). If even 100 is slow or flaky, lower the count (e.g. 50) rather than failing — a valid, smaller pool beats no pool. The exact number does not matter for a playable game.

2. ASSEMBLE + COMMIT
- Move/rename the generated `location_pool.json` into `data/location_pool.json` (per the spec's repo layout).
- Validate: it must be a valid JSON array of objects with keys image_id, lng, lat, viewer_url, thumb. Run a quick sanity check (python) and confirm it is non-empty.
- Run `git init` (if not already), `git add -A`, and `git commit` with a clear message like "GeoParty: full static game + verified location pool".

FINAL REPORT: reply with (1) the pool count you shipped, (2) the git commit hash, (3) any remaining manual steps (Firebase console rules, GitHub push, smoke test). Do NOT push to GitHub. Do NOT touch the Firebase console.
